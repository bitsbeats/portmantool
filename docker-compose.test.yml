---

services:
  database:
    image: postgres:alpine
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: 23BassSword42
    ports:
      - 127.0.0.1:5432:5432
    volumes:
      - ./types.sql:/docker-entrypoint-initdb.d/types.sql:ro

  # run `runner/runner 10s nmap 10.23.42.0/24` in ./test/scan

  scanalyzer:
    build: scanalyzer
    restart: unless-stopped
    volumes:
      - ./test/scan:/scan
